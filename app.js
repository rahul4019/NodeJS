console.log(
  process.argv
); /* prints an array containing path of the node.js executable and the path of the current file */

//* argv => provides all the arguments in an array
console.log(process.argv[2], process.argv[3]);
